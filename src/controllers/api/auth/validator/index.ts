import createUserRules from "./register-user";
import loginUserRules from "./login-user";

export default Object.freeze({
  createUserRules,
  loginUserRules,
});

export { createUserRules, loginUserRules };
