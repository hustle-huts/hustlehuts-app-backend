import registerUserRules from "./register-user";
import loginUserRules from "./login-user";

export default Object.freeze({
  registerUserRules,
  loginUserRules,
});

export { registerUserRules, loginUserRules };
